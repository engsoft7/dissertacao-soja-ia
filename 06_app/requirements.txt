# Limites de major fixados para o deploy no Streamlit Community Cloud ser
# determinístico e não puxar sozinho um major novo não testado (o que já
# causou instabilidade). As faixas incluem as versões testadas do app.
streamlit>=1.49
altair>=5.0,<7
pandas>=2.0,<3.1
numpy>=1.24,<2.6
scikit-learn>=1.3,<2

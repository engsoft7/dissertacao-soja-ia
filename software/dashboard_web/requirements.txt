# Limites de major fixados para o deploy no Streamlit Community Cloud ser
# determinístico e não puxar sozinho um major novo não testado (o que já
# causou instabilidade). As faixas incluem as versões testadas do app.
streamlit>=1.49
altair>=5.0,<7
pandas>=2.0,<3.1
numpy>=1.24,<2.6
scikit-learn>=1.3,<2
folium>=0.16
streamlit-folium>=0.20
# 'anyio' FIXADO na versão que rodava. O app quebrou (Segmentation fault) no
# Streamlit Cloud exatamente quando a instalação do folium bumpou anyio
# 4.14.1 -> 4.14.2 (anyio é o runtime assíncrono do servidor). O folium NÃO
# depende de anyio, então este pin mantém o mapa interativo funcionando sem
# reintroduzir o crash. NÃO remover.
anyio==4.14.1
gitpython==3.1.50
